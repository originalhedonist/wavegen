if(next_pulse = 0)
{
    next_pulse := floor(randomdoublebetween(3, 20));
};

if(sin(x) >= 0 and sin(xprev) < 0)
{
    sequence := sequence + 1;
    if(sequence = next_pulse)
    {
        in_pulse := 1;
    };
}
if(in_pulse = 1)
{
    if(sin(x) < 0 and sin(xprev) >= 0)
    {
        in_pulse := 0;
        next_pulse := sequence + floor(randomdoublebetween(3, 20));
    };
    sin(x)
}
else
{
    0
};